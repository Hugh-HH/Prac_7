#include "RecursiveBinarySearch.h"

bool search(std::vector<int> vec, int search) {
    

}

int recSearch(std::vector<int> vec, int search, int start, int end) {
    int pivot = std::round(list.size()/2);
    if (vec.at(pivot) == search) {
        return pivot;
    }


}